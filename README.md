# Task-amnager-app
Smart Task Management System is a Flask-based web app for registering users, managing tasks, tracking priority and status, viewing analytics, and receiving live updates through WebSockets. It uses PostgreSQL in main mode with a SQLite demo fallback for local testing.
